/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BynamePipe } from './byname.pipe';

describe('BynamePipe', () => {
  it('create an instance', () => {
    let pipe = new BynamePipe();
    expect(pipe).toBeTruthy();
  });
});
